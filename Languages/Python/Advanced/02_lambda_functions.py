# Lambda Functions and Functional Programming

# Basic lambda functions
print("=== Basic Lambda Functions ===")
# TODO: Practice creating simple lambda functions
# Example: square = lambda x: x**2

# Lambda with map()
print("\n=== Lambda with map() ===")
# TODO: Practice using lambda with map function
# Example: numbers = [1, 2, 3, 4, 5]
#          squared = list(map(lambda x: x**2, numbers))

# Lambda with filter()
print("\n=== Lambda with filter() ===")
# TODO: Practice filtering with lambda
# Example: evens = list(filter(lambda x: x % 2 == 0, numbers))

# Lambda with reduce()
print("\n=== Lambda with reduce() ===")
# from functools import reduce
# TODO: Practice using reduce with lambda
# Example: sum_all = reduce(lambda x, y: x + y, numbers)

# Lambda with sorted()
print("\n=== Lambda with sorted() ===")
# TODO: Practice custom sorting with lambda
# Example: students = [('Alice', 85), ('Bob', 90), ('Charlie', 78)]
#          sorted_by_grade = sorted(students, key=lambda x: x[1])

# Complex lambda expressions
print("\n=== Complex Lambda Expressions ===")
# TODO: Practice more complex lambda functions

if __name__ == "__main__":
    print("Practice your lambda functions here!")

